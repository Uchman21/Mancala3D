using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class GUIS : MonoBehaviour {

	// Use this for initialization

	void Start () {
		GetComponent<Text>().enabled = false;
	}
	
	// Update is called once per frame
	void Update () {
		//GetComponent<GUIText>().text="Okwe v1";
	}

}
